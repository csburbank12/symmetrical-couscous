{
  "name": "wageleak",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "node --check src/main.js && node --check api/checkout.js && node --check api/verify.js && node scripts/check.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
